<h1><%=@artist.name%></h1>
<h3>Songs</h3>

<ul>
<%@artist.songs.sort_by {|x| x.name}.each do |song|%>
    <li><a href='/songs/<%=song.slug%>'><%=song.name%></a> [<%=song.genres.map {|genre| "<a href='/genres/#{genre.slug}'>"+genre.name+"</a>"}.join(', ')%>]</li>
<%end%>
</ul>